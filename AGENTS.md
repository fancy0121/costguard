# AGENTS.md

本仓库默认由 Codex / AI Agent 协作。规则目标是：保护已有修改，最小化外部暴露，保留可复核证据，先验证再汇报。

## 1. 事实与未知

- 先读当前目录适用的 `AGENTS.md`，以及存在且相关的 `CLAUDE.md`、`README`、项目清单、架构文档和测试文档。
- 能从仓库、命令输出或远程读回确认的内容才可写成事实。
- 无法确认时必须直接写 `UNKNOWN`、`MISSING` 或 `NOT PRESENT IN REPOSITORY`；禁止用模型名称、文件名、定时器、任务 ID 或口头说明推断成功。
- 外部对话的分析、模拟结果、建议测试命令不等于本地测试或生产验证。

## 2. 任务输入

后续需求只需要提供简短任务单：目标、范围、验收标准和本次授权即可。其余仓库背景由 Codex 从本文件和仓库现状补齐；如果关键约束无法从仓库确认，先标记未知并停止越界推断。

## 3. 模型与外部复核门槛

- 发送工程任务前，必须使用 Codex 内置浏览器打开已登录的 ChatGPT，并从可见界面确认 `Intelligence = Extra High`。
- 仅在以下情况启动外部 ChatGPT Pro：跨多个模块的复杂功能、架构设计或重大重构、疑难 Bug / 安全 / 性能问题，或用户明确要求双代理协作。
- 简单任务由本地 Codex 独立完成，不启动外部 Pro。
- ChatGPT Pro、`Extra High`、本地主代理的实际模型/推理强度、`luna-worker` 是否已被调用，都必须以当前界面或运行记录为证。未读回时写 `UNKNOWN`。
- Pro 不可用、`Extra High` 不可用或达到限额时：记录“外部复核未执行”，停止重复重试；在用户未要求必须 Pro 验收时，由本地 Codex 和已验证的本地工作代理继续完成、测试和验收。
- 只有用户明确要求“必须经 ChatGPT Pro 验收”，或本地证据无法确认安全性的极高风险任务，才暂停并通知用户。

## 4. 仓库基线与已有修改

开始修改前记录：

1. 仓库根目录、当前分支、HEAD；
2. `git status`；
3. 已有差异摘要；
4. 基线差异的 SHA-256；
5. 适用规则文件和测试入口。

如果当前目录不是 Git 仓库，分别记录 `NOT PRESENT IN REPOSITORY` 和 `UNKNOWN`，不得伪造分支、HEAD 或差异哈希。

不得覆盖、撤销、清理或重置用户已有修改。只选择任务需要的最小源码范围；不顺手重构无关内容，不自动提交或推送。

## 5. 外部上下文与隐私

- 默认生成可粘贴的文本上下文包，包含项目结构、架构摘要、相关源码、当前差异、错误日志、约束和验收标准。
- 只有文本不足时才准备脱敏 ZIP。
- ZIP 必须排除 `.git`、依赖、构建产物、缓存、数据库、日志、浏览器状态、`.env`、Cookie、Token、私钥以及指向外部的符号链接。
- 上传前尽力扫描密钥，记录文件清单、大小和 SHA-256。扫描能力不足时写 `UNKNOWN`。
- 内置浏览器需要上传 ZIP 时，暂停并只请用户在浏览器中完成附件选择；不得索取密码、Cookie、验证码、Token 或私钥。
- 不得把真实用户数据、生产凭据或未授权的私有内容发送给外部模型。

## 6. 外部工程师任务包

外部任务说明必须包含：

- 背景与目标；
- 当前架构及不可破坏边界；
- 修改范围与非目标；
- 明确交付物；
- 需要设计的测试；
- 禁止执行或禁止声称的操作；
- 可逐项验收的完成标准。

多个真正独立的复杂任务使用不同对话；同一问题的修正继续原对话。记录每个对话的 URL、标题、时间和关键回复。

## 7. 外部交付要求

要求外部工程师返回：技术分析和设计决策、完整 unified diff 或完整变更文件、修改文件清单、新增依赖及理由、建议测试命令、未验证项、假设和风险。

外部回复只是一份待审补丁。不得把推测、模拟结果或代码阅读称为本地测试、生产测试或已交付。

## 8. 本地独立验收

1. 审查补丁、权限边界、依赖和锁文件。
2. 在与记录基线一致的隔离副本中应用补丁。
3. 基线包含未提交修改时，先保存对应快照并安全叠加；不得破坏用户修改。
4. 执行仓库规定且与任务相关的 lint、类型检查、单元测试、合同测试、构建和 E2E。
5. 每个失败都记录命令、退出码、错误日志、文件位置、复现步骤和正确约束。
6. 发现问题时，把最小完整修正要求反馈给原外部对话；同一问题不得新开对话掩盖历史。
7. 未完成全部适用验证时，最终状态必须是“未完成”或“部分验证”，不得声称通过。

## 9. 权限边界

允许：本地读取、脱敏打包、浏览器沟通、修改任务范围内的代码、运行非破坏性测试。

未经用户明确授权，禁止：提交、推送、创建 PR、部署、迁移数据库、修改线上配置、启用生产功能、发送外部消息、购买资源或操作真实用户数据。

## 10. 最终报告

最终报告只说重点，至少包含：

- 外部对话链接、标题和时间；
- 仓库基线与已有修改保护情况；
- 文本上下文包或 ZIP / 补丁的 SHA-256；
- 实际修改文件；
- 外部返工问题；
- 本地实际测试命令、退出码和结果；
- 未验证风险、未知项和阻塞；
- 明确说明代码是否仅为本地修改。

有长期价值的规则写入文件；未经用户明确要求，不自动提交 Git。

## CostGuard 自动模型路由（原 savetoken）

按 [SAVETOKEN_SPEC.md](./SAVETOKEN_SPEC.md) 的冻结合同执行：Sol 只处理最高难度或堵点；Terra 处理中等复杂任务；Luna 与 DeepSeek 是平级执行层；智谱只作为最后低风险备用。所有 worker 的结果必须经过主代理独立检查和相关测试；模型或 Agent 身份未从运行时确认时，标记 `UNKNOWN`。
